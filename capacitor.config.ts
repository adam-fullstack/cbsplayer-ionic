import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CBS PLayer Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
